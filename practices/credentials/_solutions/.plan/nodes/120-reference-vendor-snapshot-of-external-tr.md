# 120 — reference/ — vendor snapshot of external truth

Role: worker · Tier: fast · Phase: P2 · Deps: 020, 110

## Goal
Author the read-only external world: the did:web method spec, hosted issuer documents (including rotated keys the local repo never has), the trust-registry contract, and the webhook infra config.

## You may read
- `practices/credentials/_solutions/.plan/digests/build-contract.md`
- `practices/credentials/_solutions/.plan/digests/deps-pins.md`
- `context/cedar/generation-spec.md`
- `practices/fulfillment/reference/README.md`
- `practices/credentials/_solutions/.plan/VERIFICATION.md`
- Dependency handoffs: `practices/credentials/_solutions/.plan/handoffs/020.json`, `practices/credentials/_solutions/.plan/handoffs/110.json`

## You may write
- `practices/credentials/reference/**`
- `practices/credentials/_solutions/fixtures/**`
- `practices/credentials/_solutions/tools/**`
- `practices/credentials/_solutions/.plan/logs/120/**` (raw output)

## Acceptance criteria
- A1 (V5): reference/did-web-method.md states did:web resolution as a spec states a method: HTTPS GET of /.well-known/did.json, documents rotate, verify against the current verification method. It never points at local code
- A2 (V5): reference/infra/hosted-dids/*.json: one did.json per issuer in the contract dataset, real key material (generated by _solutions/tools/, private keys only under _solutions/fixtures/), parse with the pinned affinidi-did-common Document type (a throwaway parse check is logged)
- A3 (V6): reference/trust-registry.md (keyed on DID, checked live) and reference/infra/webhook-config.* (the real logical dispatch budget; no timeout alarm configured) and the reserved-domain note (example.com must never be a live endpoint), exactly matching the contract's values
- A4 (V8): `practices/credentials/_solutions/.plan/checks/v8-coaching-lint.py --path practices/credentials/reference` exits 0; reference/README.md says only what the directory is and that it is read-only

## Verify
Command: `python3 practices/credentials/_solutions/.plan/checks/v8-coaching-lint.py --path practices/credentials/reference`
Judged by: node 121 (reads artifacts on disk, never your summary)

## Rules
- Never run git yourself. Make state changes durable only through `bash practices/credentials/_solutions/.plan/scripts/sync.sh ...`, and run `sync.sh checkpoint` at the end of every round.
- Never make a deliberately wrong edit in `practices/credentials/`. Throwaway copies go in `.sessions/build-credentials/120/` (not durable: copy any result you need into `practices/credentials/_solutions/.plan/logs/120/`).
- Never weaken a grader or fit a check to make something pass. If the honest result is red, hand off blocked with the log path.
- Nothing you write inside the learner-cloned tree (source, tests, READMEs, reference/, tickets) may narrate a defect, a suite blind spot, or the grader (invariant 12).

## Stop rule
max_rounds: 6. On exhaustion complete as `blocked` with the last failing output path. Do not expand scope to make progress feel like it is happening.

## Handoff
`bash practices/credentials/_solutions/.plan/scripts/sync.sh complete 120 --status <status> --summary "<=200 tokens" --artifacts "..." --logs "..."`
