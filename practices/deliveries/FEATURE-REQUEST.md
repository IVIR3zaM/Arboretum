# Feature request — "Skip my next box"

From the PM:

> Customers want to **skip their next delivery** without cancelling the subscription. Add a way
> to skip the next box for a subscription. After skipping, the rest of the schedule should carry
> on as normal.

That's the whole brief — it's everything the PM wrote down. They're reachable if you need more.

The expected public surface is `DeliveryService.skipNextDelivery(...)` (currently a stub that
throws).
